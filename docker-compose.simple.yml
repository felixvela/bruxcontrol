version: '3.8'

# Versión simple sin Traefik - usar si ya tienes nginx/caddy como reverse proxy
services:
  bruxcontrol-web:
    build: .
    container_name: bruxcontrol-web
    restart: unless-stopped
    ports:
      - "8080:80"
